name: Build APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:   # lets you trigger a build manually from GitHub UI

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Decode keystore from secret
        # The keystore is stored in GitHub Secrets as base64.
        # See SETUP.md for how to generate it and what SHA-1 to register in Google Cloud.
        run: |
          echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 --decode > app/coach.keystore

      - name: Inject credentials into gradle.properties
        # These come from GitHub Secrets — never put real values in the repo.
        run: |
          echo "GOOGLE_CLIENT_ID=${{ secrets.GOOGLE_CLIENT_ID }}" >> gradle.properties
          echo "GOOGLE_CLIENT_SECRET=${{ secrets.GOOGLE_CLIENT_SECRET }}" >> gradle.properties

      - name: Make Gradle wrapper executable
        run: chmod +x ./gradlew

      - name: Build release APK
        run: ./gradlew assembleRelease
        env:
          KEYSTORE_PATH: app/coach.keystore
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}

      - name: Upload APK as build artifact
        uses: actions/upload-artifact@v4
        with:
          name: marathon-coach-app
          # After the build, go to Actions → this run → Artifacts → download the APK
          path: app/build/outputs/apk/release/app-release.apk
          retention-days: 30
